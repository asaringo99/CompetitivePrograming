#include <bits/stdc++.h>
using namespace std ;
typedef unsigned long long ll ;
#define INF (ll)1e19

int t ;

int main(){
  cin >> t ;
  for(int i = 0 ; i < t ; i++){
    ll x , y , p , q ;
    cin >> x >> y >> p >> q ;
    ll a = 2 * (x + y) , b = x + y ;
    ll c = p + q ;
    ll l = 0 , r = INF ;
    while(l - r)
  }
}
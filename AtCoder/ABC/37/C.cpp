#include <bits/stdc++.h>
using namespace std ;
typedef long long ll ;
#define rep(i,n) for(int i = 0 ; i < n ; i++)
#define rrep(i,a,b) for(int i = a ; i < b ; i++)

int n , k ;
ll A[101010] ;

int main(){
    cin >> n >> k ;
    rep(i,n) cin >> A[i] ;
    ll tmp = min(k,n-k+1) ;
}